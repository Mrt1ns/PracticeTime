name: Output Issues
on:
    issues:
      types: [opened]
jobs:
    info:
        runs-on: ubuntu-latest
        steps:
            - name: Output Context
              run: ${{toJson(github)}}        